version: '3.8'

services:
  chat:
    build: .
    container_name: chat
    restart: unless-stopped
    environment:
      TZ: ${TZ:-UTC}
    ports:
      - "8873:8873"
    env_file:
      - .env
    volumes:
      - ./data:/app/data

networks:
  chat:
    driver: bridge

